package playground;

type SpawnCubeRequest {}
type SpawnCubeResponse {}

type DeleteCubeRequest {
  EntityId cube_entity_id = 1;
}

type DeleteCubeResponse {}

component CubeSpawner
{
    id = 12011;
    list<EntityId> spawned_cubes = 1;
    uint32 num_spawned_cubes = 2; // TODO UTY-1081 remove this once lists can be emptied again

    command SpawnCubeResponse spawn_cube(SpawnCubeRequest);
    command DeleteCubeResponse delete_spawned_cube(DeleteCubeRequest);
}
